---
layout: base.njk
title: News & Updates | Cambium Arbor
---
<section class="container">
    <h1>News & Updates</h1>
    <p>The latest from the field.</p>

    <div class="grid">
        {%- for post in collections.news | reverse -%}
        <article>
            <header>
                {% if post.data.thumbnail %}
                <img src="{{ post.data.thumbnail }}" alt="{{ post.data.title }}" style="width: 100%; height: 200px; object-fit: cover; border-radius: var(--pico-border-radius) var(--pico-border-radius) 0 0;">
                {% endif %}
                <h3><a href="{{ post.url }}">{{ post.data.title }}</a></h3>
                <small>{{ post.date | date("MMMM d, y") }}</small>
            </header>
            <p>{{ post.data.description }}</p>
            <footer>
                <a href="{{ post.url }}" role="button" class="outline">Read More</a>
            </footer>
        </article>
        {%- endfor -%}
    </div>
</section>
